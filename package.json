{
  "name": "lerna-github-actions",
  "version": "1.0.0",
  "description": "lerna monorepo setup with github actions for ci",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
